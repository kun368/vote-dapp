spring:
  datasource:
    url: jdbc:mysql://votedb:3306/votedapp?characterEncoding=UTF-8&useSSL=false&autoReconnect=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: MYSQL
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect